{
  "name": "calc-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "vite --host 127.0.0.1", "build": "vite build", "preview": "vite preview --host 127.0.0.1", "test": "node --test tests/*.test.js" },
  "dependencies": { "mathjs": "^15.0.0" },
  "devDependencies": { "vite": "^7.0.0", "@playwright/test": "^1.55.0" }
}
